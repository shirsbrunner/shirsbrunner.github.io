{% extends "page.njk" %}

{% block content %}

  <div class="hero_container">
    {% if decoration %}
      <span class="header_deco" aria-hidden="true">{{ decoration }}</span>
    {% endif %}
    <h1 class="textWidthLimit">
      <span class="start_hero">{{ title }}</span>
      {#<br>
      <span class="subtitle"> {{subtitle}}</span> #}
    </h1>
    
    {#The following is the text container#}
    <div class ="textWidthLimit flow">
      {{ content | safe }} 
    </div>
  </div>
  <div class="content_container center_content_container">

    <div class="page_content flow">
      <div class="pagelist flow">
        {#{% include "partials/divider.njk" %}    #}

        <h2>From the log</h2>
        <div class = "post_previews flow ad_headerincrement">
          {# shows the latest article #}
          {% set postslist = collections.posts | head(-2) %}
            {%- for post in postslist -%}
                {% include "partials/front_postpreview.njk" %}
            {%- endfor -%}
        <div>
          <a href="/articles/">Latest log</a>
        </div>
        </div>

      </div>

      
      <div class="pagelist flow">
        {#{% include "partials/divider.njk" %}    #}

        <h2>Latest type</h2>
        <div class = "post_previews flow"> {#no headerincrement as different sizes anyway#}
          {# shows the latest typespecimen #}
          {% set typelist = collections.typefaces | head(-2) %}
            {%- for post in typelist -%}
              {% include "partials/front_typepreview.njk" %}
            {%- endfor -%}
          <div>
            <a href="/articles_typetester/">More type</a>
          </div>
        </div>
      </div>

      {#{{ postslist[0].templateContent | safe }}#}
  

    </div>
  </div>



{% endblock %}